# =============================================================================
# Handstand Tracker API — environment (copy to .env and set values)
# =============================================================================

# Required in all environments
DATABASE_URL=postgresql://localhost:5432/handstand
SESSION_SECRET=change-me-to-a-random-string

# Production: SESSION_SECRET and TRACKER_API_SECRET must be set.
# Use a long random string for SESSION_SECRET (e.g. openssl rand -hex 32).

# CORS: comma-separated origins allowed to call the API (tracker app + landing if browser calls)
ALLOWED_ORIGINS=http://localhost:3000
# Production example: https://handstand-web.onrender.com,https://your-landing.com

NODE_ENV=development
PORT=4000

# -----------------------------------------------------------------------------
# Landing page → tracker (post-purchase create user)
# Set TRACKER_API_URL on the landing page to this API base + /api/users
# e.g. https://handstand-api.onrender.com/api/users
# -----------------------------------------------------------------------------
TRACKER_API_SECRET=your-shared-secret-with-landing-page

# -----------------------------------------------------------------------------
# Set-password / forgot-password email link base URL (tracker app origin)
# Required for forgot-password emails: link in email is {TRACKER_APP_URL}/set-password?token=...
# -----------------------------------------------------------------------------
TRACKER_APP_URL=https://your-tracker-app.onrender.com
# TRACKER_LOGIN_URL=  # alternative to TRACKER_APP_URL

# -----------------------------------------------------------------------------
# Forgot-password: send email via Resend (required for production reset emails)
# Without these, forgot-password creates a token but no email is sent.
# Get API key: https://resend.com — From must be a verified domain or onboarding@resend.dev for testing.
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_xxxx
RESEND_FROM=Handstand Tracker <onboarding@yourdomain.com>
